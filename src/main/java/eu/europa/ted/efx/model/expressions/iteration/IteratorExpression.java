package eu.europa.ted.efx.model.expressions.iteration;

import eu.europa.ted.efx.model.expressions.Expression;

/**
 * Used to represent iterators (for traversing a list using a variable)
 */
public class IteratorExpression extends Expression.Impl {

  public IteratorExpression(final String script) {
    super(script);
  }
}